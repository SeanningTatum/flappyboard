# qr-first-tv-link run — 2026-07-29

## Step 1 — Baseline + kickoff

```
init.sh --baseline: typecheck PASS, 1191 tests PASS, harness-check 7/7 PASS (branch main). feat-014 registered and set in-progress; feat-015 controller-board-mirror registered planned. Plan: session thor-cyborg-pantha (owner-approved).
```

## Step 2 — Part 2: login ?next= return URL

```
session.ts: safeNextPath (rejects //evil.com, absolute/relative URLs, empties) + loginRedirectUrl; requireSession now redirects to /login?next=<path+query>. login/sign-up loaders honor validated next via redirectIfAuthenticated; both forms navigate(next ?? '/dashboard') and carry next across the login<->sign-up links. e2e auth spec updated to /login?next=. Tests: 15 passed (15) in session.test.ts.
```

## Step 3 — Part 1: QR-first /tv

```
tv.tsx: loader returns linkUrl = tvLinkUrl(request.url, code) (pure, tested: 3 tests); TvQr component (qrcode pkg, 38vmin, data-link-url attr for tests); typed code kept as 6vmin fallback beneath; instructions copy updated en+zh. Tests: 3 passed in routes/__tests__/tv.test.ts.
```

## Step 4 — Part 3: name-the-board on /link

```
link.tsx rewritten: loader does manual session check -> redirect(loginRedirectUrl(request)) preserving ?code=; code prefilled via normalizeDeviceCode(?code=). Action: intent=new -> normalizeBoardName/isValidBoardName (no-name, name_too_long) -> board.create -> approveDeviceCode (create_failed); intent=existing unchanged; success returns {name, boardId}. UI: native radio group (existing-select / new-name, no-JS safe), dead-end empty state removed, success gains link-open-controller button -> /b/:id/c. Locale keys link.* rewritten en+zh. Gate: typecheck 0, 1201 tests passed (51 files).
```
