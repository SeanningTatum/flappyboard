# pairing-experience-redesign run — 2026-07-29

## Step 1 — Redesign implemented per decision ledger

```
tv.tsx: CONSOLE tokens throughout, QR on PLATE_LIP plate, code as mono track readout (tv.codeLabel en+zh), static amber pilot lamp (inkMute when unavailable), meta color-scheme/theme-color. link.tsx rebuilt as console surface: ConsoleField (extracted to console.tsx, control.tsx now imports it), wells (WELL_LIP 48px), SegmentTrack existing/new with sr-only native radios + pure :checked CSS reveal (verified compiled in root css: group-has-[[value=new]:checked]:hidden), ink keys with 100ms press, amber lamp success, focus-visible amber rings, meta. control.tsx: meta + min-h-11 mirror toggle. Copy: segment labels shortened en+zh. Gate: typecheck 0, 1201 tests, build 0. Login deliberately left light (account door, not remote).
```

## Step 2 — browser verification walk V1-V5

```
FAIL (1 claim): all restyle rgb measurements, full pairing golden path, no-JS contract, login seam, roles, heights pass; V4.focus-ring fails — link-submit inline INK_KEY_STYLE.boxShadow (link.tsx:215) overrides focus-visible:ring utility, amber ring never renders (probe: :focus-visible matches, computed shadow = inline bevel). 0 jsErrors 0 networkErrors. 8 shots + verdict doc persisted
```

## Step 3 — re-verify E1 focus-ring fix (addendum 2)

```
PASS: outline-based FOCUS_RING composes with inline bevel — measured on link-submit/link-code/link-open-controller: :focus-visible true, outline rgb(255,204,0) 2px solid offset 2px; unfocused press shadow inset 0 -1px rgba(0,0,0,0.25) intact; happy-path pairing still flips TV to 144; verdict doc flipped to PASS (FAIL history kept)
```
