# auto-tv-link run — 2026-07-29

## Step 1 — Auto-link implemented

```
link.tsx loader auto-resolves when ?code= normalizes and manual!=1: resolveAutoLink(boardCount) pure (create/single/pick, 3 tests), create uses defaultBoardName(locale) (Living Room/客厅, 4 tests), approve with rollback discipline; failures fall to the manual form with the reason named (autoError). New linked mode: receipt panel (amber lamp, link-auto-success, link-open-controller, link-auto-manual escape to ?manual=1). tv.instructions copy updated en+zh (no more naming). GET-side-effect documented as /tv/claim precedent. Gate: typecheck 0, 1207 tests (+6).
```

## Step 2 — browser verification walk A1-A6

```
PASS: fresh-signup auto-create 'Living Room' + pair (144 tiles, 1 HttpOnly fb_device_ cookie), 1-board auto-pair with paired copy, multi-board picker, bogus-code fallback with rollback (no orphan), manual=1 escape + link-auto-manual landing, /tv regression (QR/code/amber lamp). 2 initial FAILs were driver URL-read races, re-verified PASS with waitForURL. 0 jsErrors 0 networkErrors; 5 shots + verdict doc persisted
```

## Step 3 — re-verify Greptile fixes R1-R6 (addendum)

```
PASS (13/13): receipt 302s to /link?paired=<id>&created=1 (no code in URL) with created copy; refresh + Back both still render receipt (no link-error/form); 1-board pair lands created=0 with paired copy, link-manage-boards→/boards, link-auto-manual count=0; bogus code still form+not-found, no orphan; manual=1 picker intact; 0 jsErrors 0 networkErrors. 3 shots + addendum persisted
```
