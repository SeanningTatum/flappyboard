# Verification: Auto TV Link

- **Slug**: `auto-tv-link` (feature memo: `.brain/features/auto-tv-link/auto-tv-link.md`)
- **Date**: 2026-07-29
- **Verified by**: feature-verifier subagent (Playwright CLI — headless script, bundled chromium with `--disable-extensions`; fresh TV context per scenario + separate phone contexts)
- **Base URL**: http://localhost:5173 _(dev server: started by agent via `bun run dev`; branch confirmed `feat/auto-tv-link` via `git branch --show-current`)_
- **Role**: fresh sign-up account (`fresh-<ts>@preview.local`, 0→1 boards) for A1/A2/A4/A5; `admin@preview.local` (many boards) for A3; TV contexts anonymous
- **Verdict**: ✅ PASS — all six scenarios verified with measured assertions; the two initial FAILs were driver navigation-read races, re-verified PASS with proper `waitForURL` (details below); zero jsErrors, zero networkErrors.

Run constants: A1 code **`7LKCSW`** (device cookie `fb_device_416fa929-c…`, HttpOnly); A5-escape codes `LY5LUA` (manual=1) / `FCKEGB` (auto→manual); A6 TV code `FCKEGB`; re-check codes `XPNUX4` (A1) / `5S8FXZ` (A5).

## Golden path

_A QR scan pairs the TV with zero decisions when the account is unambiguous; the picker only appears when the choice is real._

| # | Step | Expected | Observed | Screenshot | Result |
|---|------|----------|----------|------------|--------|
| 01 | Fresh account, 0 boards: TV `/tv` → phone (0 cookies) opens QR URL → login → sign-up (next carried) → register | lands back on `/link?code=`; WITHOUT interaction `link-auto-success` with "we made “Living Room” for you" copy; `link-open-controller` visible; `link-auto-manual` href `/link?code=…&manual=1`; TV flips to 144 no-reload; exactly 1 HttpOnly `fb_device_` cookie; `/boards` = exactly 1 board "Living Room" | 0 cookies; `/login?next=%2Flink%3Fcode%3D7LKCSW`; sign-up link href `/sign-up?next=%2Flink%3Fcode%3D…` (re-check: landed `/sign-up?next=%2Flink%3Fcode%3DXPNUX4`); auto-success text "All set — we made “Living Room” for you and the TV is showing it. Rename it anytime in Boards."; manual href `/link?code=7LKCSW&manual=1`; tiles=144; `fb_device_416fa929-c… httpOnly=true` (exactly one); boards `["Living Room"]` | [`01-fresh-signup-auto-created.png`](../screenshots/01-fresh-signup-auto-created.png) | ✅ |
| 02 | Same account (now 1 board), new TV + code | `link-auto-success` with PAIRED copy, not created copy; TV flips; still 1 board | "All set — the TV is now showing Living Room." (no "we made"); tiles=144; boards `["Living Room"]` | [`02-one-board-auto-paired.png`](../screenshots/02-one-board-auto-paired.png) | ✅ |
| 03 | Admin (many boards), new TV + code | PICKER renders (intent track visible), `link-auto-success` ABSENT; submit existing-first-board → manual `link-success`; TV flips | picker visible, auto-success count=0; `link-success` after submit; tiles=144 | [`03-multi-board-picker.png`](../screenshots/03-multi-board-picker.png) | ✅ |

## Error path

| # | Step | Expected failure surface | Observed | Screenshot | Result |
|---|------|--------------------------|----------|------------|--------|
| 04 | One-board account opens `/link?code=ZZZZZ9` | manual FORM renders with `link-error` (not-found copy); `link-auto-success` absent; rollback leaves no orphan | form visible; error "No TV is waiting on that code. Check it, or reload the TV for a new one."; auto-success count=0; boards still `["Living Room"]` | [`04-bogus-fallback.png`](../screenshots/04-bogus-fallback.png) | ✅ |
| 05 | `?manual=1` escape + `link-auto-manual` link | `/link?code=<real>&manual=1` renders picker not auto, code prefilled; clicking `link-auto-manual` from a real auto-success lands on picker with code prefilled and `manual=1` in URL | manual=1 visit: picker visible, auto-success count=0, prefill `LY5LUA`; escape click (re-check): href `/link?code=5S8FXZ&manual=1` → landed exactly there, prefill `5S8FXZ`, picker visible | [`05-manual-escape.png`](../screenshots/05-manual-escape.png) | ✅ |
| 06 | Regression: `/tv` surface | QR + `tv-code` fallback + amber pilot lamp intact | qr visible; `tv-code` text `FCKEGB` = code in URL; `tv-lamp` bg `rgb(255, 204, 0)` | (covered by 01 shot shape) | ✅ |

## Note on the two re-checks

The first scripted run reported FAIL on `A1.signup-carries-next` and `A5.auto-manual-escape`. Both were driver artifacts, not app behavior: the script read `page.url()` after `waitForLoadState("networkidle")`, which returns immediately when the page is already idle — before the client-side navigation the click triggers has started. A targeted re-run with `Promise.all([waitForURL(...), click(...)])` showed the app doing exactly the right thing (sign-up href `/sign-up?next=%2Flink%3Fcode%3DXPNUX4`; manual escape landing `/link?code=5S8FXZ&manual=1` with prefilled picker). The follow-on assertions in the same run had already proven both flows functionally (registration succeeded through that sign-up page; the picker rendered after the escape click). No app change was needed.

## Console

- **jsErrors** — none across all contexts (5 TV contexts, fresh phone, admin phone) and all steps.
- **networkErrors** — none.

## Findings for main thread

None. The auto-pair decision matrix (0/1/2+ boards, manual override, rollback) behaved exactly as spec'd, and all testids/copy matched (`link.auto.created` with curly quotes, `link.auto.paired`, `link.auto.manual`; `tv.instructions` no longer mentions naming — visible in the /tv shot lineage from feat-016).

## Verdict rationale

PASS. The headline flow — a brand-new account scanning a TV QR ends up paired to an auto-created "Living Room" board with no interaction past registration — was asserted end to end: redirect chain, `next` threading through sign-up, created-copy receipt with both CTAs, TV flip to exactly 144 tiles with a single HttpOnly device cookie, and exactly one board in `/boards`. The one-board auto-pair, multi-board picker, bogus-code rollback (form + named error + no orphan), both manual escapes, and the /tv regression all measured green. Zero console and network errors throughout.

---

## Addendum: Greptile-fix re-verification

_Post-ship re-verification of three Greptile review fixes: (1) `board.list` failure renders the form with an error instead of auto-creating; (2) successful auto-pair 302-redirects to `/link?paired=<boardId>&created=0|1` (spent code gone from the URL) and the receipt renders from that clean URL; (3) the receipt's "not that board?" escape replaced by `link-manage-boards` → `/boards`. `?manual=1` still forces the picker pre-pairing. Same driver pattern; branch confirmed `feat/auto-tv-link`; fresh sign-up account `greptile-<ts>@preview.local`._

- **Verdict**: ✅ PASS — all 13 re-checks green with measured values; zero jsErrors, zero networkErrors. Top verdict stays PASS.

| # | Step | Expected | Observed | Screenshot | Result |
|---|------|----------|----------|------------|--------|
| R1 | Fresh sign-up (0 boards) + waiting TV: scan → sign-up → receipt | URL is `/link?paired=<id>&created=1`, `code` NOT in URL; created copy; TV flips 144; 1 HttpOnly `fb_device_` cookie | `http://localhost:5173/link?paired=40b79747-710d-4cd6-bc08-2cd196d1edd3&created=1` (no `code=`); "All set — we made “Living Room” for you…" + OPEN THE CONTROLLER + MANAGE BOARDS; tiles=144; `fb_device_40b79747-7… httpOnly=true` (exactly one) | [`R1-receipt-clean-url.png`](../screenshots/R1-receipt-clean-url.png) | ✅ |
| R2 | Refresh-safe receipt: reload, then navigate away + Back | receipt still renders both times — no `link-error`, no manual form, same copy | reload: `link-auto-success` visible, `link-error` count=0, `link-code` count=0, same "Living Room" copy at the same clean URL; Back from `/boards`: identical | [`R2-refresh-safe.png`](../screenshots/R2-refresh-safe.png) | ✅ |
| R3 | One-board auto-pair (same account), new TV/code | `/link?paired=<id>&created=0` with paired copy; `link-manage-boards` → `/boards`; `link-auto-manual` count=0 | URL `…/link?paired=40b79747-…&created=0` (no `code=`); "All set — the TV is now showing Living Room."; manage href `/boards`; auto-manual count=0; TV flipped 144 | [`R3-manage-boards.png`](../screenshots/R3-manage-boards.png) | ✅ |
| R4 | Bogus code `/link?code=ZZZZZ9` signed-in | manual form + `link-error` not-found; no orphan | form visible; "No TV is waiting on that code. Check it, or reload the TV for a new one."; `/boards` still exactly `["Living Room"]` | — | ✅ |
| R5 | `/link?code=<real>&manual=1` | picker renders pre-pairing, code prefilled, no auto-success | picker visible; prefill `X3AQ8N`; auto-success count=0 | — | ✅ |
| R6 | Console policy | zero jsErrors; no unexpected networkErrors | jsErrors: none; networkErrors: none (across 3 TV contexts + phone) | — | ✅ |

**Rationale**: the redirect receipt works exactly as the fix intends — the spent single-use code leaves the URL at pairing time, so reload and Back re-render the receipt from `?paired=` against the account's board list instead of re-running the loader against a dead code. Created vs paired copy tracks `created=1|0`, the manage-boards CTA replaces the old escape (count=0 for `link-auto-manual`), the pre-pairing `?manual=1` picker and the bogus-code rollback are unchanged, and the headline fresh-account flow still pairs end to end (144 tiles, one HttpOnly device cookie whose name embeds the same board id as the receipt URL). Fix (1) — board.list failure → form with error — is code-reviewed in the loader (`form(boards, …)` paths) but not directly drivable in the browser without fault injection; all user-reachable paths around it verified.
