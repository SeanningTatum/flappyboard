# kiosk-display run — 2026-07-29

## Step 1 — kickoff: close out feat-012 — regression walk + Samsung recipe

```
Code complete since 2026-07-28 (kiosk.ts 22 tests, use-wake-lock 21, board-offline 16; drift/dim/watchdog constants pinned). Committed baseline fd8d654: typecheck 0, 1185 tests. Outstanding per the feature doc: (1) regression with drift active — 144 tiles, 24x6, scrollable=false; (2) socket kill shows the reconnect scrim and triggers exactly ONE reload (WATCHDOG_MS 2min, shouldReload one-shot); (3) Samsung-browser setup recipe not yet written. The 8h soak and the real-TV walk are owner-only and stay flagged.
```

## Step 2 — verify walk 1: FAIL on the one-shot reload claim, defect measured

```
Desktop-Chromium walk (verdict doc verifications/2026-07-29.md). GREEN: 144 tiles, 24x6 grid, scrollable=false with drift active (data-drift 3,0 -> 0,3 after exactly 240.1s), drift is a wrapper transform not layout, data-dimmed false at hour 22 matches isDimHour, scrim over RETAINED grid (revision unchanged), watchdog reload at +120,961ms, recovery 508ms after unblock, zero jsErrors/networkErrors. FAIL: reloaded AGAIN at a further +120,940ms — slow reload loop for the whole outage. Root cause: the reloaded latch is a page-lifetime useRef (display.tsx:258) that window.location.reload() resets; unit test only covered reloaded:true passed explicitly. Finding 2: a wedged-open-but-dead socket is invisible (page socket OPEN, status live, no watchdog for 210s observed) — only visibility-resume resync detects it; owner call pending (doc-scope or app heartbeat). Fix dispatched: sessionStorage-backed latch in kiosk.ts, one reload per outage, cleared on socket-live. Samsung recipe written: recipes/samsung-tv-setup.md + index row.
```

## Step 3 — fix + re-verify: latch moved to sessionStorage, walk 2 PASS

```
Fix: createReloadLatch in kiosk.ts (key flappyboard:watchdog-reloaded), useRef gone from display.tsx. One reload per outage; shouldReload false while latched; cleared on status==live so a separate outage re-arms; throwing/absent storage degrades to NO reload (never a crash, never an unremembered loop). +6 tests (1191 total), typecheck 0. Walk 2 PASS (same verdict doc, walk-1 FAIL kept as history): outage 1 — scrim over retained grid, counter 1->2 at +122,314ms, then NO second reload across a 300,310ms window (the claim that failed walk 1); recovery 503ms, 'REARMED 99' flapped in; outage 2 — counter 2->3 at +152,414ms proving clear-on-live re-arm, not just the latch. jsErrors/networkErrors none. 6 new screenshots (03, E5-E9; 12 total). Finding 2 stands: wedged-open-but-dead socket invisible to the watchdog — owner call (doc-scope vs heartbeat). Feature doc updated: 'one reload per outage' section rewritten honestly, files table, changelog, status shipped pending ship command. Samsung recipe recipes/samsung-tv-setup.md written + indexed.
```
