# Verification: otel-tracing — 2026-07-29

> **Provenance: this run happened upstream, not here.** Carried over with the
> feature when tracing was synced from `cf-saas-starter-react-router` 0ae0796 on
> 2026-07-31. It verifies the tracer, the OTLP encoding and the export path —
> all synced unchanged — against the *template's* dev server and its 15
> procedures. It does **not** cover flappyboard's own board/boards/tv/agent
> procedures, and no OTLP capture has been run against this repo yet. Treat that
> as still owed; see the evidence field on `feat-018`.

**Verdict**: PASS (live smoke against the dev server + 301-test unit suite)

Not a browser walk — this feature has no UI. Verified by driving the live app's tRPC surface and capturing the actual OTLP export with a local sink.

## Method

1. Local OTLP sink (Bun HTTP server) on `:4318` capturing `POST /v1/traces`.
2. `.dev.vars` with `OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318`, `OTEL_SERVICE_NAME=smoke-test` (removed after the test — file did not previously exist).
3. `bun run dev` (bound `:5176`), `bun run db:seed`.
4. Signed in as `admin@preview.local` (Better Auth email/password, origin header required), called `GET /api/trpc/admin.getUsers?batch=1&input=...`.

## Evidence — one request produced one complete trace

Captured OTLP payload (trace `3526e2215670f9d0b839603fa44a2cab`):

- Root span `trpc.admin.getUsers`, kind `2` (server), `spanId d27df3291ce7b897`, status code 1.
- Two child spans `db.query user`, kind `3` (client), both `parentSpanId: d27df3291ce7b897`, same traceId, `db.operation`/`db.entity` attributes — these are the paged `SELECT` + `COUNT` from `UserRepository.getUsers` via `tryQuery`, traced with zero repo edits.
- Resource attribute `service.name = smoke-test` (env-driven).
- Export arrived as one POST after the response (flush in the `runtime.dispose()` finalizer inside `ctx.waitUntil`).

Error path + no-op path (endpoint unset → zero fetches), buffer cap, drained-buffer, and log traceId correlation are pinned by the unit suite (`app/services/__tests__/tracing.test.ts` + extended logger/effect-utils/effect-trpc tests — 301 passing).

## Known behavior (by design)

- Imperative `loggers.X` calls outside Effect (e.g. `timingMiddleware`'s "Procedure complete") carry no traceId — only Effect-context logs correlate. Documented in `codebase/observability.md`.
- `effect-ts-enforcer` review of the full diff: clean, 0 findings.
