# phone-control run — 2026-07-27

## Step 1 — Phase 4 kicked off (opus): pairing token core + controller UI. Security-critical, so the brief mandates HMAC-SHA256 over crypto.subtle keyed with BETTER_AUTH_SECRET covering every field, a caller-supplied 'now' so expiry is deterministic in tests, a nonce for single-use, CONSTANT-TIME signature comparison, base64url encoding, and typed failure reasons (malformed/bad-signature/expired) rather than a boolean. Coordinator will review the token logic line by line before it ships.

```
feat-007 split-flap-board shipped first, so feat-008 phone-control could open without breaking the one-in-progress policy — harness-check confirms 'at most one feature in-progress, pass, 1 in-progress' and 7/7 overall. New tagged error PairingTokenInvalidError -> UNAUTHORIZED must be registered in both tagToTRPC and its test per non-negotiable #3.
```

## Step 2 — Phase 4 implementation landed (492 tests, typecheck 0, build 0, live-smoked by the agent). Coordinator reviewed app/lib/board/pairing.ts line by line as promised: PASSES review, and stronger than specified.

```
Reviewed and confirmed: HMAC-SHA256 over crypto.subtle; the signed message is prefix|boardId.length|boardId|payload so purpose and format version are authenticated (a grant presented as a pairing token fails 'malformed' before the key is consulted) and the boardId is a MAC audience with length framing, making the encoding injective — ('a','b.c') cannot collide with ('a.b','c'); verify order is structure -> signature -> claims so nothing in the payload is trusted even to say 'expired' before the MAC matches; timingSafeEqual accumulates over the full length with the length folded in; MAX_TOKEN_LENGTH 512 rejects unbounded input before crypto; clock is injected and expiry uses >=; decodeClaims is total and rejects expiresAt <= issuedAt; cookie is HttpOnly + SameSite=Lax + conditional Secure, and Path=/ is correct because writes go to /api/trpc while scoping is enforced by per-board cookie NAME plus the board id inside the MAC. Only nit: MintTokenInput exposes a nonce test seam, documented as test-only.
```

## Step 3 — Spawned an opus agent to close 5 gaps the Phase 4 agent honestly self-reported rather than hid

```
Gap 1 SECURITY: single-use pairing was enforced via the Workers Cache API, which is per-colo, evictable and A NO-OP UNDER wrangler dev LOCAL MODE — so replay protection was effectively absent in dev and only partial in prod, undercutting the one property the QR depends on. Moving the ledger into the Durable Object where check-and-set is genuinely atomic (single-threaded, keyed by board id). Gap 2: updateSettings never notified the room, so the plan's literal acceptance criterion ('mute from the phone silences the next flip on the display') could not pass. Gap 3: /api/board-ws required a session, so a grant-only phone had no live socket at all and Phase 4 worked around it by passing boardId: "" to disable the hook. Gap 4: the display mints the QR token once per page load, so a TV on the wall for 3 minutes shows a dead QR. Gap 5: toBoardMessage/gridToMessage are pure but live in components untested, violating non-negotiable #4.
```

## Step 4 — All 5 gaps closed (opus). Coordinator re-ran the gate and reviewed the live evidence rather than the summary.

```
Gate: typecheck exit 0 with 0 error TS lines, Test Files 34 passed (34) / Tests 538 passed (538), build exit 0. Gap 1 proof — the property that was actually broken now holds in local dev where the Cache API version silently did not: replay of a spent token issued NO grant cookie and returned UNAUTHORIZED with WARN [trpc] Pairing refused {reason: spent}; 6 concurrent redemptions of one token returned '200 401 401 401 401 401' (exactly one winner); the same token replayed AFTER A FULL DEV-SERVER RESTART still refused, proving it is storage-backed and not in-memory; pruning read straight out of the DO's sqlite showed 5 expired keys collapse to 1 after a fresh spend. Atomicity comes from blockConcurrencyWhile holding the input gate across the whole read->decide->write in a single-threaded per-board DO. Gap 2: TV socket received 'state revision=31 muted=true' then 'revision=32' only for setMessage — settings frames genuinely carry the SAME revision, and D1 agreed (sound_pack classic, muted 1, revision 32). Gap 3 authorisation matrix: no session+no grant on a REAL board -> 404, invented board -> 404, unrelated cookie -> 404, FORGED grant -> 401, real-board grant presented for an invented board -> 404, valid grant with no session -> 101, owner session -> 101. Non-enumerability holds: the 404/401 split depends only on what the caller sent. Gap 4: QR image changed after 50s with socket still live, revision undisturbed 31->31, grid text unchanged, refreshed token redeemable; while hidden 0 revalidations, on wake 1 immediately.
```

## Step 5 — Coordinator decisions on two items the agent left open

```
(1) The QR/sound overlays overlapping the board field's bottom edge by ~15px: leaving as-is. The agent confirmed they land on the frame's mask, never on a tile window, so it reads as a placard mounted on the bezel rather than a defect — not worth churning display.tsx for. (2) gridToMessage reporting align:'right' for an all-blank row (24 leading pad, 0 trailing): left alone deliberately. padLine([], anything) produces an identical blank row, so it is provably unobservable, and the agent recorded it in a test instead of leaving it implicit — which is the right treatment for a harmless asymmetry. Verifier spawned ALONE rather than alongside the phase 5/6 agents: their edits would hot-reload the dev server mid-walk and a flaky verdict is worse than serialising ten minutes.
```

## Step 6 — feature-verifier browser walk: PASS. Doc at .brain/features/phone-control/verifications/2026-07-27.md with 10 screenshots. Coordinator then acted on its one actionable note by adding data-muted and data-sound-pack to the display root.

```
Verdict: PASS. Golden path 6/6, error paths 2/2, jsErrors none, networkErrors none (none even on the refusal steps, because both refusals are decided server-side in the loader so the browser only ever sees a 302 or a 200). The headline claim was proven, not assumed: the controller ran in a fresh 390x844 context asserted to hold ZERO cookies, and after redeeming the token its jar held exactly one HttpOnly fb_grant_seed-board cookie and NO auth session cookie (asserted explicitly); final URL had no t= in it; control-root reported data-access=grant. From that state it sent a message, switched sound pack, muted, and re-flipped history. Revision semantics measured from the TV OWN WebSocket frames rather than inferred: grid writes moved 41->42->43 and a history re-flip 43->44, while mute plus a sound-pack change held at 42->42 with the rendered grid byte-identical, and the captured frame read type=state revision=42 muted=true. That is precisely what the apply-equal-revisions rule exists to permit. Gate after my display change: typecheck exit 0 with 0 error TS lines, Tests 538 passed (538).
```

## Step 7 — Recorded two verifier findings that are environment/test-data hygiene, not product defects, so nobody files them as bugs later

```
The verifier chased down and eliminated two harness artifacts with grep evidence: (a) a caret-color: transparent hydration mismatch that is injected by PLAYWRIGHTS OWN screenshotter (node_modules/playwright-core/lib/server/screenshotter.js:72) — grep -rn for caret-color across app/ returns nothing, and it vanished once screenshots were gated on real hydration, re-confirmed by deliberately reproducing the exact SSR state that had triggered it; (b) Vite dep-optimizer 504s on a cold dev server that break hydration and turn the login form into a native POST returning 405 — cured by a warm-up pass. Test-data drift also noted: seed-board board_snapshot history is dominated by blank grids from earlier runs and board.revision has drifted to 44, and because bun run db:seed is INSERT OR IGNORE it will not reset the drifted row. Worth a targeted reset before any future demo.
```

## Step 8 — Board management UI delegated (opus): /boards page with create + list + copyable TV URL, closing plan open question 1 (how does a board get created). Until now board.create and board.list existed as tRPC procedures with NO UI, so the only board in existence was the seeded one — which blocked the owner from testing and blocked running evals against a fresh board.

```
Partitioned around two agents already editing the repo: the new agent owns app/routes/boards/_index.tsx, app/components/boards/* (plural, since board/ singular belongs to the restyle agent), routes.ts, a NEW boards i18n namespace (board.json singular is owned by the restyle agent), i18n.d.ts/i18n.ts, and the dashboard entry point. It is explicitly forbidden from app/trpc/** because the phase 5 agent is adding a generate procedure to trpc/routes/board.ts right now, so it must build entirely on the existing create/list/get and report any procedure it wanted rather than adding one. Required proof is end-to-end rather than cosmetic: create a board through the UI, then open its /b/<id> URL and assert 144 flap tiles render, which is what makes a UI-created board demonstrably usable rather than just a row in D1.
```

## Step 9 — Board management UI landed (opus) with end-to-end proof, and coordinator reviewed the screenshot directly. Delegated the delete/rename follow-up it identified.

```
Live proof 14/14 checks: created "Kitchen TV" through the UI, list went 1 -> 2 cards, TV URL http://localhost:5173/b/48f63854-cf80-4ad2-9432-cd9bfeb9fcf1 with copy putting exactly that on the clipboard, and CRITICALLY that UI-created board /b/<id> rendered 144 flap-tile elements — so a board made through the UI is genuinely usable, not just a D1 row. A fresh sign-up account saw the empty state and creating with a blank name produced a board named flappyboard (the procedure default). Also checked desktop, 390px mobile, dark mode and the zh locale. Gate: typecheck exit 0, Test Files 36 passed (36) / Tests 574 passed (574), build exit 0. My own read of 10-boards-created.png: hierarchy is deliberate, the "TYPE THIS INTO YOUR TVS BROWSER" label is the right framing since the whole product starts with typing that URL into a TV, and the revision badges (NOTHING ON IT YET vs REVISION 44) tell you at a glance whether a board has ever been written. Copy degradation is handled properly — no clipboard API means it selects the text in place with a keyboard hint, and the address is user-select:all regardless.
```

## Step 10 — Delegated board delete + rename (sonnet) — the gap the boards agent reported rather than silently working around

```
Rationale: without delete, testing accumulates boards permanently and the previous agent had to clean local D1 by hand, so this directly blocks the users stated goal of creating boards to test and evaluate against. Security constraint made explicit in the brief: delete and rename must use requireOwnedBoard, NOT requireBoardAccess — a paired phone holding a controller grant may write TO a board but must never be able to destroy or rename someone elses board. Also required: a confirm dialog naming the board (deletion takes its whole snapshot history via the ON DELETE CASCADE on board_snapshot.boardId), and an explicit note that deleting the D1 row orphans that boards Durable Object storage — harmless, since an idle DO hibernates at no cost and board ids are UUIDs so an id is never reused, but it should be stated rather than ignored.
```

## Step 11 — Controller restyle landed (opus, 3 iterations, refero-design skill). Coordinator reviewed 07-restyle-paired.png directly.

```
The white-vs-black swatch defect is genuinely solved, not papered over: since both are fill #1f1f22 on this board (white = a white glyph on an unlit flap, black = an unlit flap), the agent stopped treating a swatch as a COLOUR sample and made it an OUTCOME sample — every swatch is a miniature flap in its window (notch, seam, stack inset, same 0.85 horizontal squeeze) showing a sample A, so white reads as a light A and black as an empty card. Second independent signal: the selected colour name is spelled out per row (YELLOW / WHITE / GREEN / RED). The preview was a black rectangle because its gaps were DARKER than its cells while the real object is the inverse (mask lighter than an unlit flap) — fixed, and iteration 2 found the load-bearing measurement: the mask takes 10% of the column pitch but 31% of the row pitch, so an equal gap was why rows did not read as rows. Refero: Elektron as primary lock (tonal ladder #000 -> #151515 -> #222226, 0px radius, off-white CTA plate with dark legend, amber #ffcc00 strictly as a state signal never an action fill), Superlative (condensed uppercase 0.14-0.20em tracking, ghost buttons as 1px hairline), teenage engineering (hairline dividers as structure, product clinically contained so the preview is framed as an object), plus a Structured iOS screen for the selection-ring-inside-the-swatch and the one-recessed-track-with-raised-chip pattern that killed the biggest web-form tell. Role rule held: board pigments appear ONLY inside tile-shaped objects, never as button fill or error bar. Also fixed a real bug found en route: the history strip auto-scrolled 16px on load because snap-start aligns to the scrollport edge, cured with scroll-px-4. All testids preserved and unrenamed. Gate: typecheck exit 0, Tests 574 passed (574), build exit 0.
```

## Step 12 — Per-cell colour control: analysed before delegating, and it needs NO schema change

```
The data model already supports it — BoardMessageRow.segments is an array of {text, color} and compileMessage already handles multi-segment rows, so the users weather example (city white on the left, temperature in a colour on the right) is expressible TODAY. The limitation is purely the editor UI, which the phase 4 agent deliberately simplified to one segment per row and reported as a simplification. Plan decision 2 had also offered a raw-cells escape hatch which the user declined at review; revisiting is unnecessary because true per-cell colour can be reached without it — a paint interaction can compile the row, recolour one cell, and run the existing gridToMessage helper (which already recovers colour runs from a grid) back to segments, so no new validator surface. Queued rather than started because it needs control.tsx, message-editor.tsx and locales/board.json, all of which the in-flight phase 6 voice agent currently owns.
```

## Step 13 — Board delete + rename landed (sonnet). It got the security question right unprompted and stated the reasoning.

```
Owner-only confirmed: delete and rename are protectedProcedure + requireOwnedBoard, never requireBoardAccess, with the agents own rationale — requireBoardAccess also accepts a controller-grant cookie, so using it here would let a phone that merely scanned a QR destroy or rename someone elses board. Cascade confirmed at app/db/schema.ts:118 where boardSnapshot.boardId is references(board.id, onDelete: cascade), so no separate cleanup query was needed. Dialog mechanics verified against the actual Radix source rather than assumed: AlertDialogAction is Dialog.Close underneath, so the handler calls preventDefault() first to skip the auto-close, meaning the dialog only closes after a SUCCESSFUL delete and stays open with a role=alert message on failure. Orphaned Durable Object storage after deletion acknowledged rather than ignored — harmless because an idle DO hibernates at no cost and board ids are UUIDs so a deleted id is never re-addressable. Repo methods both call getBoard first so a missing board is NotFoundError, never a silent no-op. Gate: typecheck exit 0, Test Files 39 passed (39) / Tests 643 passed (643), build exit 0. Live proof: created two boards, renamed one and confirmed the old name is gone, opened the confirm dialog and confirmed it names the exact board, deleted it, and confirmed the second board was untouched.
```

## Step 14 — Per-cell colour landed (opus). Coordinator then cleared its own outstanding debt: the duplicated tile pigments.

```
Weather case solved with a fourth alignment, SPREAD — UI-only, resolved by layoutToMessage into an ordinary left-aligned row whose gaps are computed WHITE spaces (separators, never lit bars), measured after normalizeText so 12° counts as 3 cells and a stray emoji as 0. Three taps instead of counting to 24, and the one-segment case is byte-for-byte the old experience. cell-paint.ts round-trips compileMessage -> recolour -> gridToMessage with two normalisations that keep the painted grid inside the set of grids the compiler can PRODUCE, which is what makes the round trip exact. Honest limit documented by the agent rather than hidden: unlit tiles cannot precede a rows first lit tile (compile.ts drops a leading separator run and alignment offers only three offsets), so paint keeps the pre-paint alignment and the leftmost cell anchors once. It caught that itself from its own screenshot after an earlier nearest-offset approach dragged already-painted tiles around on every tap. Eval on the real model: boards with a multi-colour row went 3/10 -> 6/10, multi-colour rows 8 -> 14, and 7/7 of the prompts with a real semantic axis chose sensibly (12C and -5C blue, 43 red, ON TIME green, DELAYED orange, CANCELLED red). Zero decode failures, zero repairs. NO SCHEMA CHANGE was needed, exactly as predicted. Coordinator dedup: console.tsx TILE_PIGMENTS and MASK_FILL were byte-identical copies of flap-tile.tsx TILE_COLORS (already exported) and board-frame.tsx MASK_FILL — now re-exported from the originals, so retuning the board can no longer silently desync the phone preview. My first attempt dropped the BoardColor type import and typecheck caught it (TS2304); fixed, and the gate is exactly what it is for. Gate: typecheck exit 0, Test Files 42 passed (42) / Tests 750 passed (750), build exit 0.
```
