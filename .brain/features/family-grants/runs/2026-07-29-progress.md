# family-grants run — 2026-07-29

## Step 1 — kickoff — verification of the 30-day sliding grants and per-device revoke

```
Baseline green on the branch: typecheck 0, Tests 1166 passed (1166) across 50 files, build exit 0, e2e 2 passed, harness-check 7/7. Code was already complete when this started (commit d6c3433); what feat-011 owed was the browser walk. Constants confirmed by direct measurement rather than reading the doc: DEFAULT_GRANT_TTL_SECONDS = 2592000 = 30 days (up from the 12h ceiling), DEFAULT_DEVICE_TTL_SECONDS = 15552000 = 180 days, MAX_PAIRED_DEVICES = 64. UI test ids present in board-devices.tsx: board-devices, board-device, board-device-unpair, board-device-unpair-confirm, board-device-unpair-error, board-devices-empty, board-devices-unpair-displays, board-devices-unpair-displays-confirm, board-devices-unpair-displays-error. The three claims the feature doc says a walk must show: a grant older than the old 12h ceiling still opens a socket; per-device revoke kills exactly one device and leaves the others live; a grantEpoch bump still kills all. Plus the grandfathering rule from 'As built': a grant with NO record is LIVE, not dead — reading absence as revocation would have silently un-paired every phone in existence on deploy, so revocation is an explicit tombstone keyed by the grant nonce.
```

## Step 2 — run B — cookie lifetime, TV-vs-phone epoch separation, grandfathering

```
PASS on its own board 8958b250-57eb-4eeb-a0a4-287acd052974. Two phones paired via the real QR flow, each in a fresh cookie-less context with a freshly re-scraped single-use token; each ended holding EXACTLY ONE cookie, fb_grant_<boardId>, and NO session cookie. Both wrote successfully. COOKIE LIFETIME measured with the arithmetic shown rather than eyeballed (deliberately, after an earlier worker misread 15552000 as '~15 days'): decoded the signed payload, issuedAt 1785288855520ms, expiresAt 1787880855520ms, difference 2592000000ms = 2592000s = 30.0 days exactly, matching DEFAULT_GRANT_TTL_SECONDS at pairing.ts:94. The worker explicitly noted it did NOT conflate this with DEFAULT_DEVICE_TTL_SECONDS = 180 days, which is the TV cookie. Owner's board card showed data-device-count=2 with two board-device rows, both 'Unnamed phone / Last used just now'. MY BRIEF WAS WRONG AND THE WORKER CAUGHT IT: I labelled board-devices-unpair-displays 'REVOKE ALL' and told it to expect phones to possibly die. They did not, and per the component's own doc-comment at board-devices.tsx:28-30 they never should — that control bumps deviceEpoch and hits televisions only. Its confirmation copy is explicit and good: title 'Un-pair every TV showing "family-grants-verify-B"?', body 'Every television showing this board goes back to the pairing screen. Paired phones are not affected.', buttons 'Keep them paired' / 'Un-pair TVs'. So the feature is specifically designed to resist exactly the confusion I introduced. CONSEQUENCE: the doc's third claim (a grantEpoch bump still kills all) was left UNVERIFIED by my error — the control that does that is board-card-revoke / board-card-revoke-confirm (BoardRevokeDialog). Run C dispatched on its own board to close it. GRANDFATHERING corroborated but not fully reproduced: a fresh phone C paired after the owner had already viewed the device list worked immediately, consistent with 'a grant with no record is LIVE, not dead' — true grandfathering concerns pre-deploy phones with zero DB record and is not reproducible through the live UI. jsErrors none; every /api/trpc call 200.
```

## Step 3 — run C — grantEpoch revoke kills every phone (closing the gap my run-B brief left)

```
PASS on its own board 605171b1-9078-4355-96a9-603daa8283fd. THREE phones paired via the real QR flow, each in a fresh cookie-less context with its own freshly scraped single-use token; each held only fb_grant_<boardId> and control-root[data-access]='grant' (not an owner session). All three wrote successfully first (200 setMessage x3). Owner's card showed data-device-count=3 with 3 board-device rows. Confirmation copy on board-card-revoke, verbatim: title 'Unpair every phone from "family-grants-verify-C"?', body 'Every phone that scanned this board loses control immediately, and the code on your TV stops working until the display refreshes it a few seconds later. The board keeps its name and its history. Anyone who needs it back can scan the new code.', buttons 'Keep them paired' / 'Unpair phones'. Note the copy is precise about the TV side effect — the current QR goes stale until the next re-mint because grantEpoch is inside the token's MAC — without claiming to un-pair TVs. CENTRAL ASSERTION PASSED: after one confirm, ALL THREE phones were refused, uniformly and simultaneously — 401 on setMessage x3, UI status data-status='rescan' reading 'Your access expired. Scan the code on the board again.' No phone survived. RECOVERY works: a fresh token redeemed in phone A's existing context restored data-access='grant' and a subsequent send returned 200. HARNESS TRAP WORTH KEEPING: control-root[data-access] still read 'grant' on all three phones AFTER the revoke — a stale client-side attribute, because the loader is not re-run without navigation. The server refused the write, which is what matters, but a future walk asserting only on data-access would record a false pass. Assert on the write outcome, not the attribute. jsErrors: only the three expected 401 console lines.
```

## Step 4 — run A — per-device revoke isolation (the central claim) + a half-implemented decision

```
PASS on board 7462732b-fefe-4b29-9f04-de740a478e19. Two phones paired via the real QR flow, each in a fresh cookie-less context; each held only fb_grant_<boardId>, no session cookie, data-access='grant'. Both wrote successfully. Owner saw exactly 2 board-device rows. CENTRAL CLAIM PASSED, proven by OUTCOME rather than by attribute: after un-pairing ONE row, phone A sent 'PHONE A RETRY' and got 'Sent to the board' with a 200, while phone B got a 401 and the banner 'Your access expired. Scan the code on the board again.'; after reload B's page was replaced by the expired-grant screen (title 'SCAN THE BOARD AGAIN', body 'This link has expired. Point your phone at the code on the board to take control again.', button 'TRY AGAIN') and A was still data-access='grant' and fully functional. report.json: phoneADied=false, phoneBDied=true. Rows 2 -> 1. Exactly one died, exactly one survived — the sibling grant was untouched. FINDING 1, needs an owner call before ship: run A reported rowsDistinguishable=false — both rows read identically as 'Unnamed phone / Last used just now / Un-pair', so the owner CANNOT TELL WHICH PHONE IS WHICH. This is decision 4 half-implemented, not cosmetic: board.ts:155-160 documents the field's purpose as making "un-pair Kai's phone" sayable, and the read path is wired (device.name consumed at :303, threaded through BoardAccess) but NOTHING EVER SETS IT — control.tsx:48 calls board.pair({boardId, token}) with no name and deviceName: null is hard-coded at :232 and :1059. No rename affordance exists in the owner's list either. So per-device revoke is mechanically correct and its isolation is proven, but the capability decision 4 exists to deliver is unreachable from the UI. FINDING 2, dev-only and NOT reproduced: /boards hard-crashed once on first hydration with TypeError: Cannot read properties of null (reading 'useRef') inside a Radix Dialog, surfaced as 'Oops!'; a reload and every later context loaded cleanly and it never recurred. Reads as a Vite dep-optimisation race. Recorded not dismissed — it is a crash on a route users hit — but not attributed to this feature and not fixed on speculation; worth a look during the kiosk-display soak or against a production build.
```

## Step 5 — kickoff: wire device-name capture (verification finding 1)

```
Owner call 2026-07-29: post-pairing name offer, not an interstitial, not a stated gap. Baseline green on worktree-tv-living-room with the in-flight claim->deviceName change: typecheck 0, 1166 tests, 50 files. Server side already wired at pair time (board.ts:651 recordGrant w/ normalizeDeviceName(input.name)); missing: a nameGrant path on the room/service, a mutation for an already-paired phone, and the controller UI offer. claim now returns deviceName (uncommitted) so the UI can tell unnamed from named.
```

## Step 6 — implement: post-pairing device-name offer wired end to end

```
nameGrant path added: parseNameGrantRequest + pure decideName in paired-devices.ts (7+8 tests), DO route POST /grants/name + handleNameGrant in workers/board-room.ts, BoardRoom.nameGrant service (+4 service tests). decideName: tombstoned nonce -> live:false nothing written; unknown nonce -> record created via renewRecord (grandfathered phones get named without re-pairing); existing record -> name replaced, issuedAt preserved, window slid. tRPC nameDevice mutation: publicProcedure + requireBoardAccess, grant-only (owner gets ValidationError BAD_REQUEST, not a credential error), nonce never on the wire (from caller's own grant), whitespace name refused, clearing not offered. UI: DeviceNamePrompt on /b/:boardId/c when access=grant and claim.deviceName==null; save/skip, role=alert error line, testids board-device-name-{prompt,input,save,skip,error}; en+zh locale keys. Owner list needed no change (board-devices.tsx:148 already name ?? unnamed). Gate: typecheck 0, 1185 tests (from 1166, +19).
```

## Step 7 — verify: naming delta browser walk PASS

```
PASS across 5 claims, measured on the live app (verdict doc .brain/features/family-grants/verifications/2026-07-29.md). Unnamed phone got the prompt, named phone never does (prompt count 0 after reload). Saved 'Kai's phone' appeared in the owner list; two rows now distinguishable (rowsDistinguishable true, was false). Kill shot by write outcome: un-pairing the row selected by its NAME gave Kai's phone 401+rescan while the unnamed survivor kept writing 200. Whitespace refused at two layers (client disables SAVE; replayed request measured 400); role=alert proven live via a 401 after a grantEpoch bump. jsErrors/networkErrors: only the 3 expected refusal lines. 9 screenshots added (08-14, E3-E4). Findings logged: whitespace-alert path unreachable from the real UI (client trims); revoke-all leaves dead rows in the owner list (owner call); one unreproduced dev hydration warning.
```
