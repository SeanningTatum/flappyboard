# llm-board-agent run — 2026-07-28

## Step 1 — kickoff — issue #1 rate limiting (Phase 0 of the TV living-room plan)

```
Baseline after 'bun install' in the fresh worktree: typecheck PASS, test PASS (849 passed / 44 files), harness-check PASS (7/7, brain check 9/9). NOTE: the first baseline run FAILED typecheck with 'app/trpc/client.tsx(12,14): error TS2742: The inferred type of api cannot be named without a reference to ../../../../../node_modules/@trpc/react-query/dist/getQueryKey.d-CruH3ncI.mjs'. Not a real regression — node_modules was empty in the new worktree so TS resolved the dep five levels up in the parent checkout and flagged the path non-portable. 'bun install' (734 packages) fixed it. app/trpc/client.tsx is unchanged since Initial commit. Any future worktree needs bun install before its baseline means anything. Design target located: workers/board-room.ts handleSpendNonce (line 203) is already an atomic check-and-increment under blockConcurrencyWhile with a pruneNonces companion (line 252) — the quota counter is a sibling of that method, not a new mechanism.
```

## Step 2 — issue #1 spend caps — implemented and proven live

```
Gate: typecheck exit 0; Tests 892 passed (892) across 45 files, up from the 849 baseline (+37 quota unit, +2 tagToTRPC mapping, +4 board-room service); build exit 0; e2e 2 passed; harness-check 7/7 with brain check 9/9. LIVE PROOF against a real BoardRoom DO on a dev server (throwaway bun scripts, deleted): (1) sequential — attempt 21 refused, 'Board generate limit reached. Try again in 1484s.'; (2) CONCURRENCY, the claim that justifies the DO — 30 simultaneous board.generate calls at a limit of 20 returned tally { INTERNAL_SERVER_ERROR: 20, TOO_MANY_REQUESTS: 10 }, exactly 20 admitted, no increments lost; (3) per-spender — owner exhausted at 20, then a phone holding ONLY a redeemed controller grant and no session got its own full 20 and was refused at 21; (4) the re-pairing bypass — a 3rd grant took the board to 60/60 and a 4th grant's VERY FIRST call was refused by the board ceiling despite a brand-new nonce with zero personal spend. The 20 INTERNAL_SERVER_ERRORs are the expected success shape locally: no ANTHROPIC_API_KEY in a fresh worktree, so a call that clears the cap then fails at the model. GOTCHA WORTH KEEPING: app/lib/effect-trpc.ts gates its switch on a runtime APP_ERROR_TAGS Set that is NOT type-checked against the AppError union — the class, the union member and the case all compiled while RateLimitError still returned 'Internal Server Error', because isAppError returned false and the switch was never reached. recipes/add-tagged-error.md did not mention this step; it does now. NOT PROVEN: /api/transcribe's 429 (same spendQuota call and ledger, but reaching it needs the remote AI binding), fail-closed against a live broken DO (unit-tested at the service boundary only), and window rollover. feat-009 stays in-progress — it still owes a browser walk of the actual voice-to-board path.
```
